rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // v0 deny-all stub — replaced by per-user rules in V0.3.
    // No client has any Firestore access until V0.3 ships.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
