{
  "name": "usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'usage-dashboard: build is local-only (run scripts/usage-dashboard/build.sh; CI has no ~/.claude data)'"
  },
  "devDependencies": {
    "ccusage": "0.8.0",
    "jsdom": "^25.0.0"
  }
}
