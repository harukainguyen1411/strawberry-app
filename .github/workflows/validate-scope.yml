name: Validate Scope

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate-scope:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Report scope (informational only)
        run: |
          BASE="${{ github.event.pull_request.base.sha }}"
          HEAD="${{ github.event.pull_request.head.sha }}"
          CHANGED=$(git diff --name-only "$BASE" "$HEAD")
          echo "=== Changed files ==="
          echo "$CHANGED"
          echo ""
          echo "=== Files in apps/myapps/ ==="
          echo "$CHANGED" | grep '^apps/myapps/' || echo "(none)"
          echo ""
          echo "=== Files outside apps/myapps/ ==="
          echo "$CHANGED" | grep -v '^apps/myapps/' || echo "(none)"
          echo ""
          echo "Scope check complete (informational only — this step never blocks)"
          exit 0
